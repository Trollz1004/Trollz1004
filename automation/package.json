{
  "name": "team-claude-ai-engine",
  "version": "1.0.0",
  "description": "24/7 Self-Maintaining AI Content Engine",
  "main": "ai-content-engine.js",
  "scripts": {
    "start": "node ai-content-engine.js",
    "docker": "docker-compose -f docker-compose-ai.yml up -d",
    "deploy-aws": "sam build && sam deploy --stack-name team-claude-ai-engine",
    "logs": "docker-compose -f docker-compose-ai.yml logs -f"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "@aws-sdk/client-bedrock-runtime": "^3.500.0",
    "axios": "^1.6.0",
    "node-schedule": "^2.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
