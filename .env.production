# YouAndINotAI - Production Environment Configuration
# This file is used by Cloud Run to configure the application
# Actual secret values are managed via Google Cloud Secret Manager

# Database Configuration (managed via Cloud SQL connector)
DATABASE_URL=postgresql://youandinotai_user:PASSWORD_FROM_SECRET_MANAGER@/youandinotai?host=/cloudsql/pelagic-bison-476817-k7:us-east1:youandinotai-db
REDIS_URL=redis://REDIS_HOST:6379

# Application Settings
NODE_ENV=production
PORT=8080

# JWT Configuration (managed via Secret Manager)
# JWT_SECRET and JWT_REFRESH_SECRET are injected at runtime from Secret Manager

# Square Production Configuration (managed via Secret Manager)
# SQUARE_ACCESS_TOKEN - Production access token from Square Dashboard
# SQUARE_LOCATION_ID - Your Square location ID
# SQUARE_APP_ID - Your Square application ID

# Google Gemini AI (managed via Secret Manager)
# GEMINI_API_KEY - Production API key from Google AI Studio

# Azure Cognitive Services (managed via Secret Manager)
# AZURE_FACE_KEY - Production key from Azure Portal
# AZURE_FACE_ENDPOINT - Azure Face API endpoint

# Email Configuration (managed via Secret Manager)
# GMAIL_USER - Gmail address for sending emails
# GMAIL_PASSWORD - Gmail app-specific password

# GCP Project Configuration
GCP_PROJECT_ID=pelagic-bison-476817-k7
GCP_REGION=us-east1

# Application URL
APP_URL=https://youandinotai.com

# Feature Flags
ENABLE_AI_SAFETY_AGENT=true
ENABLE_DATE_CONCIERGE=true
ENABLE_RELATIONSHIP_COACH=true
ENABLE_TRUST_SCORE=true

# Note: All sensitive credentials are managed via Google Cloud Secret Manager
# and injected at runtime. Never store actual secrets in this file.
